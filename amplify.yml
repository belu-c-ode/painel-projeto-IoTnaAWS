version: 1

frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - npm install -g @ionic/cli
    build:
      commands:
        - ionic build
  artifacts:
    baseDirectory: www
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*